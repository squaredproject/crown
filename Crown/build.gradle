apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'code', include: ['*.jar'])
}

sourceSets {
    main {
        java {
            srcDirs = ['.']
        }
        resources {
            srcDirs = ['data']
        }
    }
}

